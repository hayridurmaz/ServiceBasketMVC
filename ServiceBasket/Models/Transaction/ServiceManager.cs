using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ServiceBasket.Models.Transaction
{
    public class ServiceManager
        //BURDA KALDIN!!!
    {      /*public static bool AddNewUser(Service newService)
        {
            // Verify that the book doesn't already exist
            Service oldService = ServicePersistence.GetService(newService.UserId);
            // oldBook should be null, if this is a new book
            if (oldService != null)
            {
                return false;
            }

            return UserPersistence.AddUser(newUser);
        }*/
    }
}